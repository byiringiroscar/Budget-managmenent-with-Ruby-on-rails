<p style="color: green"><%= notice %></p>

<h1>spends:</h1>

<div id="spends">
  <% @spends.each do |spend| %>
    <%= render spend %>
    <p>
      <%= link_to "Show this spend", category_spend_path(@category, spend) %>
    </p>
  <% end %>
</div>

<%= link_to "New spend", new_category_spend_path %>
