<p style="color: green"><%= notice %></p>

<h1>Entities</h1>

<div id="entities">
  <% @entities.each do |spend| %>
    <%= render spend %>
    <p>
      <%= link_to "Show this spend", spend %>
    </p>
  <% end %>
</div>

<%= link_to "New spend", new_spend_path %>
